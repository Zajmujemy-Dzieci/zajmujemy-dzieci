import React, { useState } from "react";

type SpecialPopupProps = {
  text: string;
  onClose: () => void;
};

export default function SpecialPopupComponent({
  text,
  onClose,
}: SpecialPopupProps) {
  return (
    
  );
}